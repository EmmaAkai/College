;termino cuando apreto f10
EOI EQU 20H
IMR EQU 21H
INT0 EQU 24H
INT1 EQU 25H


CONT EQU 10H
COMP EQU 11H

ORG 1000H
  MSG DB "No debo programar en assembly "
  nuevalinea DB 13
  MSG_FIN DB ?
  FLAG DB 0

ORG 3000H
  MANEJADOR:
	 MOV AL,0
	 OUT CONT,AL

	 MOV BX, OFFSET MSG
	 MOV AL, OFFSET MSG_FIN - OFFSET MSG
	 INT 7

	 MOV AL,20H
	 OUT EOI,AL
  IRET

ORG 3200H
  MANEJADORF10:
    MOV FLAG,1

    MOV AL,20H
	  OUT EOI,AL
  IRET

ORG 32
 DIR_MANEJADOR DW 3000H
ORG 60
  DIR_MANEJADORF10 DW 3200H

ORG 2000H
 CLI
  MOV AL,11111100B
  OUT IMR,AL

  MOV AL,15
  OUT INT0,AL

  MOV AL,8
  OUT INT1,AL

  MOV AL,0
  OUT CONT,AL

  MOV AL,5
  OUT COMP,AL
 STI

 LOOP: CMP FLAG,1
       JNZ LOOP
END